# Fact: `start_mcp_server()` creates `factory` which cr...

**Category**: WORKFLOW
**Fact ID**: FACT-WORKFLOW-038beec
**Created**: 2026-02-07T17:05:08Z
**Source Session**: 457a257e-59f1-4fc0-9955-cb067f260982

## Fact Statement

`start_mcp_server()` creates `factory` which creates agent internally. Signal handler needs to call agent's shutdown.

## Keywords

`start_mcp_server()`, creates, `factory`, which, creates, agent, internally, signal, handler, needs
